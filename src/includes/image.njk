{% macro image(src, desc, source_list, class) %}
    <picture>
        {% for src in source_list %}
            <source srcset="{{ src }}" />
        {% endfor %}
        <img src="{{ src }}" alt="{{ desc }}" class="{{ class }}">
    </picture>
{% endmacro %}